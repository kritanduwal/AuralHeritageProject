node_modules/

# Third-party HRTF data, fetched by hand — see HRIR/README.md.
# Freely redistributable, but not ours to carry and large: the one tool that
# reads it takes a path to it rather than expecting it in the tree. Nothing the
# browser loads lives here, so the whole directory stays out.
HRIR/**
!HRIR/README.md
